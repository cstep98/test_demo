name: Move branch contents to folder in main

on:
  push:
    branches:
      - '**'   # Trigger on any branch push

jobs:
  sync-branch-to-main:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Ensure the full history is fetched

      # Set up Git credentials
      - name: Set up Git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      # Get the current branch name
      - name: Get branch name
        id: vars
        run: echo "branch_name=$(echo ${GITHUB_REF##*/})" >> $GITHUB_ENV

      # Check out the main branch
      - name: Check out main branch
        run: git checkout main

      # Create a folder named after the branch and copy the files
      - name: Create folder and copy files
        run: |
          branch_name="${{ env.branch_name }}"
          mkdir -p "./${branch_name}"
          git checkout ${{ env.branch_name }} -- .
          # Exclude the folder from being moved into itself
          shopt -s extglob
          mv !("${branch_name}") "./${branch_name}/"
          git add .

      # Commit changes to the main branch
      - name: Commit changes
        run: |
          git commit -m "Move contents of branch ${{ env.branch_name }} to folder ${{ env.branch_name }} in main"
          git push origin main
